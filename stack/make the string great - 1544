class Solution:
    def makeGood(self, s: str) -> str:
        stack = []
        for letter in s:
            if stack and stack[-1] != letter and stack[-1].upper() == letter.upper():
                stack.pop()
            else:
                stack.append(letter)
        
        if stack:
            return ''.join(stack)
        else:
            return ''
